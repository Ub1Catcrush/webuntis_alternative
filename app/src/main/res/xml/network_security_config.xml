<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Release: only trust system CAs — user-installed CAs (e.g. proxy certs) are rejected -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <!-- Debug: also trust user-installed CAs so developers can inspect traffic with a proxy -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
